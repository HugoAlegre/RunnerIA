/** Versión visible del Runner: número, canal y build (sin prefijos decorativos). */
export const RUNNER_VERSION = '0.0.12';
export const RUNNER_CHANNEL = 'beta';
export const RUNNER_BUILD = '20260910';

/** Leyenda única: «0.0.12 beta build 20260910» */
export const RUNNER_VERSION_LABEL = `${RUNNER_VERSION} ${RUNNER_CHANNEL} build ${RUNNER_BUILD}`;
